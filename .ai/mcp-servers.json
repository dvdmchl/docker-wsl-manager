{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$comment": "Common MCP (Model Context Protocol) server configurations for all AI agents",
  "mcpServers": {
    "github": {
      "$comment": "GitHub MCP server configuration. Verify the httpUrl is correct for your environment.",
      "httpUrl": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer $GITHUB_MCP_PAT"
      }
    }
  }
}
